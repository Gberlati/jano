<div class="container p-4 pt-8">
    <h1 class="text-2xl text-gray-900 font-bold p-2">Carga masiva de productos</h1>

    <div class="max-w-sm rounded overflow-hidden shadow-lg pt-4">
        <div class="px-6 py-4">
        <div class="font-bold text-xl mb-2">Como cargar productos</div>
        <p class="text-gray-700 text-base">
            1. Subir todas las imágenes de productos.
        <br>2. Por cada imagen, cargar 
        </p>
    </div>
</div>

<%= form_with model: @product do |form| %>
        
        <%= form.label :name %>
        <%= form.text_field :name %>
        
        <div class="field">
            <%= form.label :images %>
            <%= form.file_field :images, multiple: true %>
        </div>

         <%= form.submit "Cargar" %>
    <% end  %>